# Copy this file to ".env" and fill in your keys. NEVER commit the real .env.
# .env is already gitignored.
#
# Phase 0: you do NOT need these yet. The hello-world script uses public data.
# When you create keys (Phase 0.4), start with READ-ONLY permissions:
#   - Query Funds / Query Open Orders / Query Ledger:  ENABLED
#   - Create & Modify Orders (trading):                DISABLED for now
#   - Withdraw Funds:                                  ALWAYS DISABLED

KRAKEN_API_KEY=
KRAKEN_API_SECRET=
